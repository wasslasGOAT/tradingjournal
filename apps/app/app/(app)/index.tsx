import { DashboardScreen } from '@/features/dashboard/DashboardScreen';

/** Dashboard (M1-8) : premier écran affiché à l'ouverture de l'app (ARCHITECTURE §6.1). */
export default function DashboardRoute() {
  return <DashboardScreen />;
}
