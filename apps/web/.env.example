# Copier en `.env` (jamais versionné, voir .gitignore racine) et renseigner
# avec les valeurs du projet Supabase de dev (ADR-020). Clé publique
# (anon/publishable) uniquement — jamais de clé `service_role`/`sb_secret_*`
# côté client (CLAUDE.md, pnpm check:secrets).
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
