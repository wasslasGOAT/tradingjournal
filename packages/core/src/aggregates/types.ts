export type { TradeRecord } from '../stats';

/** Premier jour de la semaine (préférence `week_starts_on`, DATA_MODEL `preferences`). `0` = dimanche, `1` = lundi (convention `date-fns`). */
export type WeekStartsOn = 0 | 1;
